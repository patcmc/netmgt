<div class="max-w-lg mx-auto p-6 bg-white rounded-lg shadow-md">
  <turbo-frame id="<%= dom_id(@user_contact) %>">
    <%= render Form::UserContactComponent.new(user_contact: @user_contact, url:
    app_contact_path(@user_contact)) %>
  </turbo-frame>
</div>
